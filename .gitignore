# === SEKRETY — najwazniejsza linijka w tym pliku ===
.env
.env.local
*.key
# Lokalne sekrety Streamlita — na Streamlit Cloud ustawiane w panelu, nie w repo.
.streamlit/secrets.toml

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# === Dane zrodlowe ===
# PDF-y (kilkanascie MB) pobiera skrypt ingest.py — nie trzymamy ich w repo.
data/pdfs/

# UWAGA: katalog `index/` NIE jest ignorowany, i to celowo.
# Zbudowany indeks FAISS (kilka MB) jest commitowany, bo aplikacja w chmurze
# ma go tylko odczytac. Gdyby go tam nie bylo, kazdy start Streamlita oznaczalby
# ~8 minut przeliczania i setki zapytan do API na darmowym limicie.

# === System ===
.DS_Store
